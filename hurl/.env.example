# Environment configuration for ABAP ADT API Hurl scripts
# Copy this file to .env and modify the values for your SAP system

# SAP System Configuration
SAP_HOST=https://vhcalnplci.local:8000
SAP_USERNAME=DEVELOPER
SAP_PASSWORD=your_password_here
SAP_CLIENT=100

# Optional: Language setting (defaults to EN in scripts)
SAP_LANGUAGE=EN

# Optional: Hurl settings
HURL_VERBOSE=false
HURL_INSECURE=true

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. Source the file: source .env
# 4. Or use with hurl: hurl --variables-file .env script.hurl
